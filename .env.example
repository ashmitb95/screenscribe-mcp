# Claude — frame descriptions + answering
ANTHROPIC_API_KEY=sk-ant-...
# Gemini — watches the video to pick frames. Strongly recommended; without it,
# frame selection falls back to transcript-based picking.
GEMINI_API_KEY=
